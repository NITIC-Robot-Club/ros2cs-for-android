--- a/src/ros2/rmw_fastrtps/rmw_fastrtps_cpp/src/type_support_common.hpp
+++ b/src/ros2/rmw_fastrtps/rmw_fastrtps_cpp/src/type_support_common.hpp
@@ -37,14 +37,18 @@
 inline std::string
 _create_type_name(
-  std::string message_namespace,
-  std::string message_name)
+  std::string message_namespace,
+  std::string message_name)
 {
-  std::ostringstream ss;
+  // std::ostringstream ss;
+  std::string ret = "";
   if (!message_namespace.empty()) {
-    ss << message_namespace << "::";
+    ret += message_namespace + "::";
+    // ss << message_namespace << "::";
   }
-  ss << "dds_::" << message_name << "_";
-  return ss.str();
+  ret += "dds_::" + message_name + "_";
+  // return ss.str();
+  return ret;
 }
